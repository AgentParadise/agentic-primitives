# Simple Task

List all files in the current directory using `ls -la`.

Then create a file called `test-output.txt` with the text "Hello, World!".

Finally, read the file back and confirm its contents.
