id: {{id}}
kind: command
category: {{category}}
domain: {{#if domain}}{{domain}}{{else}}general{{/if}}
summary: "{{summary}}"
{{#if tags}}
tags:
{{#each tags}}
  - {{this}}
{{/each}}
{{/if}}

defaults:
  preferred_models:
    - claude/sonnet

context_usage:
  as_system: false
  as_user: true
  as_overlay: false

tools: []

inputs: {}