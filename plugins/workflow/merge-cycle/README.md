# Workflow Merge Cycle Plugin

Full merge cycle orchestrator with QA, CI, and review gates.

## Installation

```json
{ "plugins": { "workflow/merge-cycle": true } }
```

## Commands
- `merge-cycle.md` — Full merge cycle from local changes to merged PR
