# DevOps Config Plugin

Centralized configuration management with type-safe environment variables.

## Installation

```json
{ "plugins": { "devops/config": true } }
```

## Skills
- `/devops-config:centralized-configuration` — Type-safe, self-documenting environment configuration with idempotent sync

## Resources
- Python reference implementations (settings.py, generate_env.py, github.py)
